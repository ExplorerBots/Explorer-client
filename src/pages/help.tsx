import Head from 'next/head';
import { FC } from 'react';

const HelpPage: FC = () => {
   return (
      <>
         <Head>
            <title>EBots - Помощь</title>
         </Head>
      </>
   );
};

export default HelpPage;
